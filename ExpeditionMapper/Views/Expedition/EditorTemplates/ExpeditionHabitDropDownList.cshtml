@model string

@(Html.Kendo().DropDownList()
    .Name("Habit")  //Important, must match the column's name
    .Value(Model)
    .SelectedIndex(0)
    .BindTo(new string[] { "Select Habit", "This is a habit", "This is a second habit", "This is a third habit" }))