@model string

@(Html.Kendo().DropDownList()
      .Name("Habit") //Important, must match the column's name
      .Value(Model)
      .SelectedIndex(0)
      .BindTo(new[] {"Select Habit", "Synergize", "Begin With the End in Mind", "Be Proactive"}))